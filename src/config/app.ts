const app = {
  name: "Askr",
  description: "Design System Platform — Create, manage, and publish design",
  keywords: ["design system", "design systems", "css variables", "npm package"],
  feedbackFormUrl: process.env.NEXT_PUBLIC_FEEDBACK_FORM_URL ?? "",
};

export default app;
